# this is a program which counts to 10, then halts
wra 0i
sta 0i

loop:
    # add 1 to counter
    lda 0i
    wrb 1i
    add
    sta 0i

    # if counter is 10 goto end
    wrb 10i
    xor
    giz end

    # otherwise return back to loop
    goto loop
end:
    lda 0i
    hlt